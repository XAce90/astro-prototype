---
const response = await fetch(`https://the-one-api.dev/v2/book`, {
  headers: {
    Authorization: `token ${import.meta.env.PUBLIC_AUTH_TOKEN}`,
  }
})
const data = await response.json();
const books = data.docs;
---
<style>
  .lotr-books {}
  ul {
    list-style: none;
    padding: 0;
    
    display: flex;
    grid-column-gap: 1rem;
  }
</style>
<div class="lotr-books">
  <h2>The Book Series</h2>
  <p>Learn more about your favorite Lord of the Rings book.</p>
  <ul>
    {books.map((book => (
      <li class="book" style="">
        <a href={`/books/${book._id}`}>
          <img src={`/images/books/${book._id}.jpg`} alt={book.name}>
        </a>
      </li>
    )))}
  </ul>
</div>